version: '3.8'

services:
  api:
    build: ./backend/DGII.API
    ports:
      - "5000:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
